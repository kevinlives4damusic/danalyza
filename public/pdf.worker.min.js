
    // Minimal PDF.js worker
    globalThis.pdfjsWorker = {
      WorkerMessageHandler: {
        setup(handler) {
          handler.on('test', () => {
            return true;
          });
        }
      }
    };